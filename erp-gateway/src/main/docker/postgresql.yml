version: '2'
services:
  erpgate-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/erpGate/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=erpGate
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
